# React boilerplate

React boilerplate for ReactJs apps

## Instructions

```shell
npm install
```

```shell
npm start
```

or, if you use `yarn`:

```shell
yarn run start
```

and for production mode:

```shell
npm build
```

or

```shell
yarn run build
```
